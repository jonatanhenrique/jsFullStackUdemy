const nome = 'Rebeca';
const concatena = 'Olá' + nome + '!';

const template = `Olá
${nome}
!`;

console.log(template);

console.log(`1 + 1 =  ${1+1}`);

const up = texto => texto.toUpperCase(); // up recebe texto e converte para upper
console.log (`Ei... ${up('cuidado')}!`);